# 后端配置说明

代码已全部完成，前 3 步你已经做完了。这份文档只讲**剩下的第 4 步**，以及怎么判断每一步是否成功。

| 步骤 | 内容 | 状态 |
|---|---|---|
| 1 | 注册 Supabase，建项目 | 已完成 |
| 2 | 填 `config.js`（URL + anon key） | 已完成 |
| 3 | SQL Editor 执行 `supabase-schema.sql` | 已完成 |
| 4 | **配置自定义 SMTP（解除发信限流）** | **待做** |

---

## 为什么必须做第 4 步

Supabase 免费层自带的发信服务限制极严：

- **每小时只能发 2 封**（所有类型邮件合计，超限返回 `429 over_email_send_rate_limit`）
- 实测中，仅仅做连通性探测就撞上了这个限制

你在手机和电脑上各登一次就用光了。第 3 次登录会收不到邮件，页面提示"发送太频繁"。

接上 QQ 邮箱 SMTP 后，限额提高到 **30 封/小时**，且可发给任意地址。免费，10 分钟。

---

## 第 1 小步：拿到 QQ 邮箱授权码

1. 登录 <https://mail.qq.com>
2. 顶部 **设置** → **账户**
3. 往下找到 **POP3/IMAP/SMTP/Exchange/CardDAV/CalDAV服务**
4. 开启 **IMAP/SMTP服务**
5. 按提示用密保手机发一条短信，页面会返回一串 **16 位授权码**
   - 形如 `abcdefghijklmnop`
   - **这不是你的 QQ 密码**，是一串独立生成的字母串
   - 只显示一次，先复制下来

> 如果没开通过，QQ 邮箱会要求先设置"账户安全"里的密保手机。

---

## 第 2 小步：填进 Supabase

1. 打开 <https://supabase.com/dashboard/project/jumdscnowxsicowfgwnx>
2. 左侧 **Project Settings**（齿轮图标）→ **Authentication** → 左侧 **SMTP Settings**
3. 打开 **Enable Custom SMTP**
4. 按下表填写：

| 字段 | 填什么 |
|---|---|
| Sender email | 你的 QQ 邮箱完整地址，如 `123456@qq.com` |
| Sender name | `英语口语练习`（随便写） |
| Host | `smtp.qq.com` |
| Port number | `587` |
| Username | 你的 QQ 邮箱完整地址（同 Sender email） |
| Password | 上一步拿到的 **16 位授权码** |

5. 点 **Save**，等约 10 秒生效

> Port 填 587（STARTTLS）。465 是 SSL 直连，Supabase 这个表单对 465 支持不稳定，用 587 更稳。

---

## 第 3 小步：确认邮件里是数字验证码

这一步决定你收到的是「一串数字」还是「一个链接」。我们要的是数字。

1. Supabase 控制台 → **Authentication** → **Emails**
2. 找到 **Magic Link** 模板，点进去
3. 检查正文里有没有 `{{ .Token }}`
   - **有** → 不用改，收到的邮件里会有 6 位数字码
   - **没有** → 在模板正文里加一行：

```html
<p>你的验证码是：<strong>{{ .Token }}</strong></p>
```

4. Save

> 如果你愿意，也可以直接点邮件里的链接登录（Supabase 默认行为），但我们的界面设计是输数字码，所以建议加上。

---

## 验证是否成功

在网站右上角点 **登录 / 同步**，输入你的邮箱，点发送验证码。

| 现象 | 含义 |
|---|---|
| 收到含 6 位数字的邮件 | 全部配置正确 |
| 提示"发送太频繁" | SMTP 还没生效，或配额刚被用完，等 1 小时 |
| 提示"该邮箱不在白名单" | SMTP 没保存成功，回第 2 小步检查 |
| 收到的是链接不是数字 | 回第 3 小步加 `{{ .Token }}` |

登录成功后状态条会变成 **云端已登录 · 你的邮箱**。此时在手机浏览器打开同一网址、用**同一个邮箱**登录，两边的练习记录就合并了。

---

## 关于账号本身

**没有独立的注册流程——也不需要。** 首次输入邮箱发验证码时，账号会自动创建（`shouldCreateUser: true` 且服务端 `disable_signup = false`，已实测确认）。所以"注册"和"登录"是同一个入口，输邮箱 → 收码 → 填码，一次完成。

登录态持久化在浏览器本地（刷新、关标签页都不掉线）。换设备才需要重新收码。

---

## 两个长期注意事项

1. **免费项目连续 1 周无请求会自动暂停。** 去控制台点一下 Restore 即可恢复，**数据不会丢**。
2. **免费层没有自动备份**（Automatic Backups 是 Pro 功能）。练习记录只有一份，建议偶尔点「我的记录」里的 **导出 JSON** 存一份到本地。
